#:schema https://mise.jdx.dev/schema/mise.json

[env]
GITHUB_REPOSITORY = "liblaf/github-bot"
GITHUB_REPOSITORY_OWNER = "liblaf"
GITHUB_TOKEN = { default = '{{ "" if env?.CI == "true" else exec(command="gh auth token") }}', redact = true }
